import React from 'react'

export function MetaText () {
  return <p>Copy this meta tag into your site's <code>&lt;head&gt;</code> to apply the revshare. Make sure it's the only Web Monetization meta tag on your site.</p>
}
