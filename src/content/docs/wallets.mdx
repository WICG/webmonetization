---
title: Web Monetization-compatible wallets
lastUpdated: 2026-08-20
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import { LinkOut } from '@interledger/docs-design-system'

You must have an account with a compatible wallet provider to send or receive Web Monetization payments. It can take up to 24 hours for a wallet provider to confirm your identity when setting up a new account.

Be sure to select a wallet that's available in your **region** and supports your preferred **currency**.

## Wallet providers

| Provider                                                | Available in | Supported currencies                                                    |
| ------------------------------------------------------- | ------------ | ----------------------------------------------------------------------- |
| <LinkOut href="https://gatehub.net">GateHub\*</LinkOut> | USA, EU      | $ <abbr title="US dollars">USD</abbr>, € <abbr title="euros">EUR</abbr> |

<sup>* Select *Hosted* wallet during setup</sup>

## Interledger test wallet

The Interledger test wallet provides a great way to test Web Monetization without using real money. The test wallet is an open source app developed and maintained by the Interledger Foundation. [Create an account on the Interledger test network](/guides/test-wallet-sign-up) to get started.

## GateHub

:::note
GateHub wallet users can only send and receive from other GateHub wallet users.
:::

<details>

<summary>GateHub payment addresses</summary>

GateHub payment addresses are both wallet addresses and payment pointers. This means your address can begin with `https://` or with `$`. **The two are interchangeable.**

GateHub's UI displays your payment address in both formats. For example, your address appears as a _payment pointer_ in the **Interledger Settings** section of the **Advanced** screen.

<img
  src="/img/docs/gatehub/advanced-interledger-settings02.png"
  alt="Interledger settings section showing the full addresses of the user's two payment pointers"
  class="img-outline"
  style="max-width: min(100%, 600px)"
/>

The **Edit payment pointer** screen uses the label _Wallet Address_ for your payment pointer.

<img
  src="/img/docs/gatehub/edit-pp.png"
  alt="Edit payment pointer screen with a wallet address label"
  class="img-outline"
  style="max-width: min(100%, 500px)"
/>

In some areas of their UI, you'll find your address labeled as a payment pointer, but the address will begin with `https://` instead of `$`.

<img
  src="/img/docs/gatehub/pp-with-https.png"
  alt="Example of a payment pointer beginning with https://"
  class="img-outline"
  style="max-width: min(100%, 300px)"
/>

</details>

<details>

<summary>Find your payment address</summary>

    1. Select your wallet, then click **Advanced**.
    2. Scroll down to **Interledger Settings**.
        <img src="/img/docs/gatehub/advanced-interledger-settings01.png" alt="Interledger Settings section showing the Interledger button and text that reads 2 payment pointers." class="img-outline" style="max-width: min(100%, 500px)"/>
    3. Click the **Interledger** button.
    4. Copy your payment address.
        <img src="/img/docs/gatehub/advanced-interledger-settings02.png" alt="Interledger settings section showing the full addresses of the user's two payment pointers" class="img-outline" style="max-width: min(100%, 600px)"/>

</details>
